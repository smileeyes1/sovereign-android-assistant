{
  "name": "hakim-browser-mcp",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Authenticated MCP gateway between ChatGPT/Codex and Hakim Browser on Android.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "check": "node --check src/relay.mjs && node --check src/server.mjs",
    "test": "npm run check && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.0",
    "express": "^5.1.0",
    "zod": "^3.25.0"
  }
}
